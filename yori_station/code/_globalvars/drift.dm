/*
	GLOBAL STATE FOR THE DRIFT MECHANIC
*/
GLOBAL_VAR_INIT(parallax_drift_movment_dir, NORTH)// sets a "default" animation to the parallax, so we can make it 'drift' slowly, but this will not effect the parallax settings of an area (so shuttels work).
//GLOBAL_VAR_INIT(parallax_drift_movment_speed, 3)
